extends Node2D

## Boundary system simplified - no longer needed!
## Player now just checks if tiles exist instead of placing collision tiles

func _ready() -> void:
	pass  # Boundary placement disabled - tile existence check is enough
